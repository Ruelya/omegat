// SPDX-License-Identifier: GPL-3.0-or-later

// One generated-catalog matrix is intentionally shared by Linux, Windows, and
// macOS runners. Keep the historical filename as the implementation location
// so existing Linux invocations continue to exercise the identical driver.
await import("./packaged-writer-catalog-linux.mjs");
