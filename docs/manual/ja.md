# OmegaT — 取扱説明書(U)

翻訳メモリ・用語集・タグ付き文書向けの、キーボード優先ワークステーション。

## Install

- Linux: deb / rpm / tar.gz (OmegaT)
- Windows: NSIS
- macOS: DMG

OmegaT sidecar: `omegat-sidecar`. UI locale: `ja`.

## 新規作成(N) / 開く(O)

1. 開く(O) — `omegat.project`
2. 新規作成(N) — 原文ファイル言語：, 訳文ファイル言語：, 文単位の分節化(N)

`source/` · `target/` · `omegat/project_save.tmx` · `tm/` · `glossary/` · `dictionary/`

## 編集

- 保存(S) (`Ctrl+S`)
- 訳文ファイルを生成(K) (`Ctrl+D`)
- 検索(S) / 置換
- 次の分節 / 前の分節
- 入力補完: 用語集, 定型文の設定, 文字一覧, 履歴補完, 履歴予測

翻訳対象ファイル一覧(F), 参考訳文, 用語集, 辞書, 機械翻訳, メモ帳, コメント, 代替翻訳によるセグメントのハイライト, プロジェクト設定(P)

## 環境設定(P)

一般, 外観, 字体設定, 配色, 編集, 表示, ユーザーレベルのファイルフィルター..., ユーザーレベルの分節化規則..., キーボードショートカット, スクリプト, 言語検査, ユーザーレベルの外部検索, チームプロジェクトをダウンロード

## 整合ツール(A)

HEAPWISE / PARSEWISE / ID · Viterbi ≠ Forward-Backward · 作成 TMX

## チームプロジェクトをダウンロード

git / svn / http / file · あなたの変更を維持 / リモートの変更を選択

## MediaWiki から原文ファイル追加(G) / MED プロジェクトを開く / スクリプト(G)

MediaWiki から原文ファイル追加(G) · MED プロジェクトを開く · スクリプト(G) · `確定(O)`

Java HTML: `docs/manual/java-html.md`.
